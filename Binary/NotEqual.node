description: "Checks if two values are not equal"
group: Binary
name: NotEqual
extends: Binary/Binary

edges:
  result:
    description: 'The remainder of the division of left by right'
    direction: output
    type: boolean

source: |
  ${OUTPUT.result = left ~= right}
