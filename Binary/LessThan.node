description: "Checks if left is less than right"
group: Binary
name: LessThan
extends: Binary/Binary

edges:
  result:
    description: 'True if left is less than right'
    direction: output
    type: boolean

source: |
  ${OUTPUT.result = left < right}
